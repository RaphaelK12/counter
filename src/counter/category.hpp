#ifndef counter_category
#define counter_category

// ::counter::category::common

namespace counter
 {
  namespace category
   {

    struct common
     {
     // empty;
     };

   }
 }

#endif
